//During the test the env variable is set to test
process.env.NODE_ENV = 'test';

const chai = require('chai');
const should = chai.should();

describe('Authentication', () => {
    describe('Validade token', () => {
        it('Should validade token', (done) => {
            done();
        });
    });
});